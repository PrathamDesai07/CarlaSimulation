# Pinned dependencies verified working on L40S / RTX 5090
#
# mmcv: built from source in Dockerfile for Blackwell/Ada GPU support.
# At eval time, model_eval.py uses pure PyTorch/torchvision fallbacks
# and does NOT import mmcv at all. The source build is kept for:
#   - model.py (training) if ever needed
#   - backward compatibility with any script that calls mmcv ops
#
# Build instructions (already in Dockerfile):
#   git clone https://github.com/open-mmlab/mmcv.git
#   cd mmcv && git checkout v1.7.0
#   MMCV_WITH_OPS=1 TORCH_CUDA_ARCH_LIST="8.0;8.6;8.9;9.0" pip install -e .
#
# torch & torchvision installed before this file for Blackwell GPU support
numpy==1.24.4
opencv-python==4.8.1.78
Pillow==10.2.0
pyyaml==6.0.1
carla==0.9.16
tqdm==4.66.1
scipy==1.12.0
timm>=0.9.0
dictor==0.1.9
scikit-image==0.22.0
Shapely==2.0.1
ujson==5.8.0
einops==0.7.0
diskcache==5.6.3
hydra-core==1.3.2
py_trees==0.8.3
networkx==2.8.8
psutil==5.9.8
xmlschema==2.5.0
lxml==4.9.4
tabulate==0.9.0
transforms3d==0.4.1
pygame==2.5.2
ephem==4.1.5
